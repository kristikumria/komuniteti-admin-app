import { theme, type AppTheme } from './theme';
import { ThemeProvider } from './ThemeProvider';
import elevation, { ElevationLevel, getElevationStyle } from './elevation';

export {
  theme,
  ThemeProvider,
  elevation,
  ElevationLevel,
  getElevationStyle
};

export type { AppTheme }; 